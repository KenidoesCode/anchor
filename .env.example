# Copy to .env for local development. Never commit real secrets (CLAUDE.md / PRD §10).
# Slice 1 uses a local Postgres only. Production is AWS RDS ap-southeast-1 (PRD §12.2).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/greensafe
